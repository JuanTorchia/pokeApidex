language: node_js

node_js:
  - "12"

script:
    - npm test
    - npm run build

env: 
  CI: false


deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: main